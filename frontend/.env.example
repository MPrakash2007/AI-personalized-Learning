# CodeOrbit Frontend Environment Variables (Optional)
# In production on Vercel, API calls default to '/api' automatically.
# NOTE: OPENAI_API_KEY is configured strictly on the backend / Vercel Serverless environment.
# Never expose OpenAI or database keys in frontend environment variables.
# VITE_API_URL=http://127.0.0.1:8000
